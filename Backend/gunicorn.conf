bind = "0.0.0.0:8000"  # Bind to all network interfaces on port 8000
workers = 2  # Number of worker processes
reload = True  # Automatically reload workers when code changes
timeout = 30  # Timeout for worker processes